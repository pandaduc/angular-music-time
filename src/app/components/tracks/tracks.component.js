import template from './tracks.html';
import controller from './tracks.controller';

let tracksComponent = {
    restrict: 'E',
    bindings: {},
    template,
    controller,
    controllerAs: 'vm'
}

export default tracksComponent;